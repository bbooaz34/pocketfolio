# Pocketfolio · תיעוד הניסיונות לפתרון מחירי חציון eBay

עדכון אחרון: 19.09.2026 · סטטוס: **בעיה פתוחה — מחפשים כיוון למקור נתונים בר-קיימא**

---

## הבעיה

להציג לכל קלף מדורג PSA שווי לפי **חציון מכירות אמיתיות ב-eBay עבור הדירוג הספציפי**
(PSA 9 ≠ PSA 10 ≠ raw), תחת האילוצים של האפליקציה:

- אפליקציה סטטית לחלוטין (GitHub Pages, ללא backend וללא שרת).
- עלות חודשית ~0.
- הדפדפן חסום ב-CORS מול רוב המקורות.
- תמיכה גם בקלפים יפניים (חלק מהאחזקות הן הדפסות יפניות).

בלי מקור מדורג, האפליקציה נופלת להערכה: `מחיר raw × מקדם דירוג` (GRADE_MULT), שהיא
לא מדויקת — זו הייתה תלונת המקור ("המספרים קופצים ולא תואמים את השוק").

---

## ניסיון 1 — PokemonPriceTracker (PPT) ישירות מהדפדפן

**מה נבנה:** מפתח API אישי נשמר בקליינט; קריאות ל-
`/api/v2/cards?search=...&includeEbay=true`; המחיר נלקח מ-
`ebay.salesByGrade.psa<N>.medianPrice`.

**מה עבד:**
- מחירי חציון אמיתיים לפי דירוג — בדיוק הנתון הרצוי.
- תמיכה ביפנית אומתה: `language=japanese` מחזיר קטלוג יפני כולל מכירות.

**הבעיות שנתקלנו בהן (וכיצד טופלו):**

| בעיה | מה קרה | טיפול |
|---|---|---|
| מודל חיוב לפי שורות | קרדיט לכל **שורת תשובה**, ×2 עם `includeEbay`. קריאה עם `limit=10` = עד 20 קרדיטים | הורדנו limits פר-ניסיון |
| מכסה חינמית קטנה | 100 קרדיטים ליום (מתאפס 00:00 UTC = 03:00 ישראל) — נשרף אחרי רענונים בודדים | backoff עד האיפוס + קאש |
| מד מכסה מטעה | הוצג "11 מתוך 100" ובפועל 429 — ספרנו *בקשות* במקום *קרדיטים* | המד הוסב לאומדן קרדיטים (שורות×2) |
| רגרסיית medians | קאש שפג + re-fetch עם limit קטן החזיר תשובה בלי הקלף → דרס ערכים טובים במחיר raw | "sticky medians" — ערך מדורג טוב לא נדרס בערך גרוע |

**המסקנה:** מקור הנתונים מצוין; המודל שבו **הקליינט** צורך אותו לא בר-קיימא:
כל פתיחת אפליקציה שורפת קרדיטים, אין שליטה על הקצב, ו-100 קרדיטים ליום לא
מספיקים אפילו ל-3 קלפים עם כמה רענונים.

---

## ניסיון 2 — Cloudflare Worker אישי כפרוקסי

**מה נבנה:** worker אישי (`proxy/prices-proxy.js`) שמנתב
`/api/v2/*` → PPT, וכן `/cert/<n>` (דפי תעודות PSA) ו-`/news` (RSS חדשות).

**מה זה פתר:** CORS וניתוב יציב (דפי PSA והפידים חסומים לדפדפן ישירות).

**מה זה לא פתר:** את הקרדיטים — המפתח עדיין נספר על כל קריאה. פרוקסי לא משנה
את מודל החיוב.

**סטטוס:** ה-worker נשאר בשימוש **לתעודות PSA ולחדשות בלבד**. נתיב המחירים בו
כבר לא בשימוש ע"י הקליינט.

---

## ניסיון 3 (הנוכחי) — ארכיטקטורת Snapshot יומי (POCKETFOLIO-PRICING.md)

**הרעיון:** להעביר את כל צריכת ה-API ל-CI. GitHub Action רץ **פעם ביום**, מתמחר
את הקלפים שב-`data/watchlist.json`, ומקומיט JSON סטטי
(`data/latest.json`, `data/snapshots/<date>.json`, `data/index.json`).
הקליינט רק קורא קובץ — אפס מפתחות, אפס מכסות, עובד אופליין, ומאפשר "מסע בזמן"
בין תאריכים.

**מה מומש (פרוס ועובד):**
- `scripts/build-snapshot.mjs` — הבילדר: PriceCharting כמקור ראשי, PPT כ-fallback,
  מזהי מוצרים נשמרים ב-`data/pc-map.json`, שומר בטיחות נגד snapshot מתכווץ.
- `scripts/prune-snapshots.mjs` — שמירת היסטוריה (90 יום יומי, 1.5 שנים חודשי).
- `.github/workflows/snapshot.yml` — ריצה יומית 04:00 UTC + הפעלה ידנית.
- הקליינט: סולם החלטה (נעוץ → snapshot → ידני ממתין → הערכת raw), שורות מקור,
  קרוסלת תאריכים, צ'יפ "עודכן אוטומטית", כפתור נעיצה 📌.

### 🛑 החסם שנתגלה — התמחור של PriceCharting

- ה-API של PriceCharting (וה-CSV היומי) זמינים **רק במנוי Legendary — ‎$50 לחודש**.
- מנוי **Collector לא כולל גישת API** בכלל.
- ‎$600 בשנה על תיק של קלפים בודדים — לא הגיוני.
- ה-probe (`scripts/pricecharting-probe.mjs`) מעולם לא הורץ עם טוקן אמיתי — אין
  טעם להריץ בלי כוונה לשלם.

### מה שכן עובד כבר עכשיו, בלי PriceCharting

הבילדר תוכנן מראש לרוץ גם עם `PPT_TOKEN` בלבד (PriceCharting הוא אופציונלי).
במצב הזה העלות היומית:

- קריאה אחת לקלף, `limit=5`, `includeEbay=true` → **עד 10 קרדיטים לקלף**.
- עם 100 קרדיטים חינם ליום: **~10 קלפים ביום בבטחה** במצב הנוכחי.
- כוונון פשוט (`limit=2` בבילדר) מוריד ל-≈4 קרדיטים לקלף → **~25 קלפים ביום**.
- הצריכה קבועה ופעם ביום — לא תלויה בכמה פעמים פותחים את האפליקציה.

כלומר: **הארכיטקטורה עצמה כבר פותרת את בעיית הקרדיטים לתיק קטן**, גם במסלול
החינמי של PPT. החסם הוא גדילה (watchlist גדול) ותלות במקור יחיד.

---

## כיוונים אפשריים להמשך (לחשיבה)

| כיוון | עלות | איכות נתונים | סיכון/מאמץ |
|---|---|---|---|
| **A. להישאר על PPT חינמי + כוונון הבילדר** (limit קטן, קאש מזהים כמו pc-map) | ‎$0 | חציוני eBay אמיתיים לפי דירוג | נמוך — שינוי קטן בבילדר; מגבלה ~25-50 קלפים |
| **B. מנוי PPT בתשלום** | לבדוק מחירון — ייתכן tier זול מ-$50 | כנ"ל, בלי מגבלת גדילה | נמוך; תלות במקור יחיד |
| **C. PSA API (Auction Prices Realized)** | חינם (יש tier חינמי מוגבל יומית) | מחירי מכירה אמיתיים לפי דירוג, רשמי של PSA; לא רק eBay | לוודא שה-tier החינמי כולל APR ולא רק אימות תעודות; מיפוי קלף→SpecID |
| **D. eBay רשמי — Marketplace Insights API** | חינם טכנית | נתוני sold אמיתיים 90 יום | **קשה** — limited release, דורש אישור מיוחד מ-eBay שקשה לקבל |
| **E. 130point.com** | חינם | נתוני sold טובים | אין API רשמי → scraping דרך ה-worker; שביר ובעייתי מול ToS |
| **F. סריקת eBay sold ישירות** | חינם | — | נגד ה-ToS של eBay, נשבר כל הזמן — **לא מומלץ** |
| **G. הזנה ידנית + נעיצה 📌** | ‎$0 | תלוי במשתמש | כבר קיים באפליקציה כרשת ביטחון |

**הערה חשובה:** יהיה המקור אשר יהיה — הוא מתחבר **רק לבילדר**
(`scripts/build-snapshot.mjs`). הקליינט, פורמט ה-snapshot, סולם ההחלטה, הנעיצה
ומסע-בזמן לא משתנים. החלפת מקור = כתיבת פונקציית `priceWithX()` אחת.

---

## לקחים טכניים (שלא נחזור עליהם)

1. **PPT מחייב לפי שורות תשובה, ×2 עם includeEbay** — תמיד לצמצם `limit` ולסנן
   בצד שלנו לפי מספר קלף.
2. **אסור לדרוס ערך מדורג טוב בערך גרוע** — קאש שפג הוא לא סיבה לרדת ל-raw
   (sticky medians / הבילדר לא כותב snapshot ריק או מכווץ >40%).
3. **מזהים נפתרים פעם אחת ונשמרים** (`data/pc-map.json`) — חיפוש לפי שם הוא יקר
   ורועש; אותו עיקרון יחול על כל מקור עתידי.
4. **snapshots הם immutable** — קובץ מתוארך לא נכתב מחדש; "היום" תמיד דרך
   `latest.json`.
5. **פרוקסי לא פותר מודל חיוב** — הוא פותר רק CORS וניתוב.
6. שדות ה-API של PriceCharting לקלפים לא תואמים את שמותיהם
   (`graded-price`=PSA 9, `manual-only-price`=PSA 10 וכו') — ממופה ב-
   `PC_GRADE_FIELD` בבילדר וב-POCKETFOLIO-PRICING.md §2.
